import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["nextjs.org", "example.com"], // Add your allowed image domains here  
  }
};

export default nextConfig;
